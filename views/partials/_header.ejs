<!DOCTYPE html>
<head style="font-size:20px; padding:5px;">
  <h1>TinyApp</h1>
  <a href= http://localhost:8080/login>Login</a>
  <a href= http://localhost:8080/register>Register</a>
  <% if (user) { %>
    <form action="/logout" method="POST" style="margin:5px;">
      <input type="submit" value="Sign Out"> Currently signed in as: <u><%= user %></u>
    </form>
  <% } %>
</head>

